---
const { category, count } = Astro.props;
---
<a href={`/wiki/${category}`} class="block p-6 bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow">
    <h2 class="text-2xl font-semibold mb-2">{category}</h2>
    <p class="text-gray-600">{count} note{count !== 1 ? 's' : ''}</p>
</a>
